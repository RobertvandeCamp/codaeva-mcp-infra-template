[project]
# This id is auto-updated by `supabase link --project-ref <ref>` — no manual replacement needed
id = "placeholder"

[db]
major_version = 15

[db.seed]
enabled = false

[auth]
site_url = "http://localhost:5173"
additional_redirect_urls = ["http://localhost:5173"]
jwt_expiry = 3600

[auth.hook.custom_access_token]
enabled = true
uri = "pg-functions://{{SUPABASE_SCHEMA}}/custom_access_token_hook"
